var submit = function () {
    var x = $('#email').val();
    
    if (x == null || x == "") {
        alert("Email must be valid.");
        return false;
    }
    else {
        alert("Thanks so much for supporting Fish are Friends! We can't wait to help the Earth get better together");
    }
}